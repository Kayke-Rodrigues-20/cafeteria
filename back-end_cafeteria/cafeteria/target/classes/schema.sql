CREATE TABLE IF NOT EXISTS cafe (
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    nome VARCHAR (100) NOT NULL,
    torra VARCHAR(15) NOT NULL,
    ml INT NOT NULL,
    preco DECIMAL(5,2) NOT NULL,
    descricao VARCHAR(100)
);